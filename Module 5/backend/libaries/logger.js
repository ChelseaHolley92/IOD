Class Logger {
    constructor(Referrer, id) {
        this.referrer = referre
        this.id =id


          log  = (value) => {
            console.log('[${this.referrer}]:${value})'
            }

            
            
            module.exports = Logger